#include<stdio.h>
int main()
{
   double area , d ;
    printf("enter the length all side = ");
    scanf("%lf",&d);
    area = (pow(d,2))/2;
    printf("enter the area of a square = \n%lf",area);
    return 0;

}
